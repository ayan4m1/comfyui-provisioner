{
  "$id": "https://raw.githubusercontent.com/ayan4m1/comfyui-provisioner/main/module.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Module",
  "type": "object",
  "description": "A set of resources for a template",
  "properties": {
    "name": {
      "description": "The short name for the module",
      "type": "string",
      "exclusiveMinimum": 0
    },
    "description": {
      "description": "The long description for the module",
      "type": "string"
    },
    "template": {
      "description": "The template that this module is meant for",
      "type": "string",
      "exclusiveMinimum": 0
    },
    "files": {
      "description": "An object with lists of URLs for type of file exposed by the template",
      "type": "object",
      "exclusiveMinimum": 0,
      "patternProperties": {
        "^[a-z0-9_]+$": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    }
  },
  "required": ["name", "template", "files"]
}
