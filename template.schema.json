{
  "$id": "https://raw.githubusercontent.com/ayan4m1/comfyui-provisioner/main/template.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Template",
  "type": "object",
  "description": "A template used to deploy a service to Vast.ai",
  "properties": {
    "description": {
      "description": "The long-form description for the template",
      "type": "string"
    },
    "environment": {
      "description": "Environment variables to define during deployment",
      "type": "object"
    },
    "hash": {
      "description": "The Vast.ai template hash to use for deployment",
      "type": "string"
    },
    "name": {
      "description": "The short name for the template",
      "exclusiveMinimum": 0,
      "type": "string"
    },
    "runType": {
      "description": "The mode to run the Vast.ai instance with",
      "type": "string",
      "enum": ["ssh", "jupyter", "args"]
    },
    "script": {
      "description": "A provisioning script to run",
      "type": "string"
    },
    "size": {
      "description": "The disk allocation for the template",
      "exclusiveMinimum": 0,
      "type": "integer"
    },
    "tag": {
      "description": "A Docker image tag to use for deployment",
      "type": "string"
    }
  },
  "required": ["name", "hash", "size"],
  "oneOf": [
    {
      "required": ["hash"]
    },
    {
      "required": ["tag", "runType"]
    }
  ]
}
